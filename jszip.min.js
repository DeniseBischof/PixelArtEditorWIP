/*! JSZip stub for offline packaging */
window.JSZip = class {
  constructor(){ this._files = []; }
  file(name, data, opts){ this._files.push({name,data,opts}); }
  async generateAsync(){ const blob = new Blob(this._files.map(f=>[f.name, "\n" ]), {type:'text/plain'}); return blob; }
};
